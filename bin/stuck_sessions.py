#!/usr/bin/env python3
"""Stub — signatures only, no behaviour yet. Replaced by the real watch in the next commit."""

KIND = "stuck_session"


def last_tool_use(lines):
    return None


def stuck_now(agents, registry, probe, now, stale_after):
    return {}, 0


def scan(path, agents, registry, probe, now, stale_after):
    return []
