{
  "name": "parallel-worktree-run",
  "description": "Spin up fully running, independent copies of a repo (own git worktree + branch + own dev stack + own ports) and dispatch background subagents into them, all from one Claude Code session.",
  "version": "1.0.0",
  "author": {
    "name": "thanhtoan0499"
  }
}
